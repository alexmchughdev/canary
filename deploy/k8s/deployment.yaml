apiVersion: apps/v1
kind: Deployment
metadata:
  name: foghorn
  namespace: foghorn
  labels:
    app.kubernetes.io/name: foghorn
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: foghorn
  template:
    metadata:
      labels:
        app.kubernetes.io/name: foghorn
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: /metrics
    spec:
      serviceAccountName: foghorn
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: foghorn
          image: ghcr.io/alexmchughdev/foghorn:latest
          imagePullPolicy: IfNotPresent
          args: ["-config", "/etc/foghorn/foghorn.yaml"]
          ports:
            - name: metrics
              containerPort: 9090
          envFrom:
            - secretRef:
                name: foghorn-slack
          volumeMounts:
            - name: config
              mountPath: /etc/foghorn
              readOnly: true
            - name: data
              mountPath: /var/lib/foghorn
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: config
          configMap:
            name: foghorn-config
        - name: data
          persistentVolumeClaim:
            claimName: foghorn-data
