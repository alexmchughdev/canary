# Slack tokens. See slack/manifest.yaml for the app setup.
SLACK_APP_TOKEN=xapp-...
SLACK_BOT_TOKEN=xoxb-...

# Bearer token for the HTTP API. Generate with:
#   openssl rand -hex 32
FOGHORN_API_TOKEN=
